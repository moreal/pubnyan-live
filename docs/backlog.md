# Backlog

Work queue shared by humans, the Planner agent, and the worker loop. Items are taken top to bottom. States: `[ ]` open, `[~]` claimed by the running worker (do not edit), `[x]` done (in the commit that completed it), `[!]` failed (reason in the bullet below it; fix the text and reopen). Every item ends with a `Done when:` sentence naming an executable check; the Reviewer verifies it, and every item still ends with `npm run verify` green and a look at the contact sheet.

## Phase 4: Lottie

- [x] **export-lottie: static frame.** `packages/export-lottie/src/index.ts` with `exportLottie(rig, clip): LottieJson`. One shape layer per part (parent via `parent` index), each with a single path shape (`ty: "sh"`) built from the rig path's cubic segments and a fill (`ty: "fl"`). Transform anchor = pivot. Register `lottieTarget` in `packages/verify/src/cli.ts`; its `renderFrame` loads lottie-web from `node_modules/lottie-web/build/player/lottie.min.js` into the page (inline the script text), calls `goToAndStop(t * 1000, false)`. Parity on `spinner` at t=0 passes.
- [x] **export-lottie: transform keyframes.** Position, rotation, scale, opacity tracks become animated properties (`a: 1`) with bezier tangents from `EASES` (`o` and `i` handles: `o = {x: [x1], y: [y1]}`, `i = {x: [x2], y: [y2]}` on the outgoing keyframe). Parity on `spinner` and `idle` passes.
- [x] **export-lottie: shape keyframes.** Morphable shape tracks animate the path; incompatible ones become one layer per expression with opacity keyframes (mirror `export-svg`). Parity passes on a test clip that morphs `normal -> angry -> normal` on `mouth`.
- [x] **motion/machine.ts.** Write the pubnyan machine from the spec (`expression` enum, `react` trigger, `loading` bool; layers idle, expression, reaction) with `validateMachine` in `npm run verify`. States reference clips that exist at the time; add the clips first.
- [x] **.lottie bundle.** `dist/lottie/pubnyan.lottie` (zip with `manifest.json`, `animations/*.json`, and a dotLottie state machine derived from `motion/machine.ts`). Load it with `@lottiefiles/dotlottie-web` in the page and confirm the listed animations and inputs.

## Phase 5: Rive

- [x] **export-rive: key table.** Script `packages/export-rive/scripts/generate-keys.ts` that clones `rive-app/rive-runtime` at a pinned commit (record it in the script) with a sparse checkout of `include/rive/generated/` and parses every `*_base.hpp` for `typeKey` and `*PropertyKey` constants into `packages/export-rive/src/keys.generated.ts` (`{ [ClassName]: { typeKey, properties: { name: key } } }`). Property backing types are inferred from the C++ field type (`float` -> double, `uint32_t`/`int` -> uint, `bool` -> bool, `std::string` -> string, `ColorInt` -> color) and emitted alongside. Unit-test the parser on a checked-in sample header.
- [x] **export-rive: writer core.** `packages/export-rive/src/writer.ts`: `RivWriter` with `varuint`, `string`, `float32`, `color`, and `object(typeKey, [[propKey, value]])` following https://rive.app/docs/runtimes/advanced-topic/format (header "RIVE", major 7, minor 0, file id, ToC of used property keys, 2-bit backing-type bitmap, objects each ending with a 0 terminator). Spike: one Backboard, one Artboard (width, height, name), one Shape with a Rectangle and a Fill + SolidColor. Load the bytes with `@rive-app/canvas` in the page; `rive.artboardNames` returns the artboard. Use `rivinfo` from the runtime repo if a load fails silently.
- [x] **export-rive: rig shapes.** Every part as Shape > Path (PointsPath with CubicDetachedVertex objects from the rig's C segments) + Fill > SolidColor, parented through `parentId` to a Node per part (pivot handled by Node x/y plus path offset). Register `riveTarget` (render via `@rive-app/canvas` on a `<canvas>` at artboard size, `rive.scrub(animationName, t)`). Parity on a static frame passes.
- [x] **export-rive: linear animations.** One LinearAnimation per clip with KeyedObject > KeyedProperty > KeyFrameDouble for x, y, rotation, scaleX, scaleY, opacity, using CubicEaseInterpolator objects for eases. Parity on `spinner` and `idle` passes.
- [x] **export-rive: shape keyframes.** Morphs via keyed vertex properties; crossfade via opacity on per-expression shapes.
- [x] **export-rive: state machine.** StateMachine with StateMachineNumber/Bool/Trigger inputs, one StateMachineLayer per machine layer, AnimationState per state, StateTransition with TransitionNumberCondition / TransitionBoolCondition / TransitionTriggerCondition, EntryState wired. Load in the runtime and assert `stateMachineInputs()` names match `motion/machine.ts`.

## Phase 6: Video

- [x] **export-video.** `packages/export-video`: render each clip through the svg target at `clip.fps` to PNG frames in a temp dir, then `ffmpeg` to `dist/video/<clip>.mp4` (h264, yuv420p), `.webp` (animated), `.gif` (palette pass). Not a parity target; the verify CLI just runs it.

## Phase 7 (done): agent loop

- [x] **Flue agents.** `src/agents/director.ts` (opus; delegates through `defineSubagent`, commits, marks items done) with the sonnet delegates `src/agents/implementer.ts` and `src/agents/reviewer.ts`; tools in `src/tools/`: `read_backlog`, `mark_done`, `run_checks` (`npm run check` through `harness.sandbox`), `git_commit` (runs the checks, then commits on `agent/backlog`); skills in `src/skills/{rig-reference,clip-dsl,pubnyan-motion,exporter-package}/SKILL.md`. `npx flue run src/agents/director.ts -m next --id pubnyan` completes one backlog item end to end.
- [x] **Agent loop scripts.** `scripts/agent-loop.mjs` (`npm run agent -- <max>`) runs the director once per item until it replies `BACKLOG EMPTY`, an item fails, or `max` items are done; it refuses to start on a dirty tree, time-boxes each run, and resolves credentials from `.env` or a pi subscription OAuth token (`src/tools/anthropic-auth.ts`). `.github/workflows/agent.yml` runs it on demand and opens a PR from `agent/backlog`.

## Animation work (for the agents)

- [x] **Expression transitions.** Clips `to-angry`, `to-curious`, `to-cry`, `to-shy` (0.4 s, non-loop) morphing/crossfading eyes, mouth, face, tears from `normal`, and the reverse clips. Wire them into `motion/machine.ts`'s expression layer.
- [x] **Reactions.** One-shot clips `nod` (body position y dip, 0.6 s), `tilt` (body rotation ±6°, 0.8 s), `ear-twitch` (not possible on the fused silhouette: instead a quick body scale-x squash, 0.3 s), `tail-flick` (ring-gap-r quick scale, 0.4 s). Wire into the reaction layer.
- [x] **Better morphs.** Redraw eye and mouth expression paths with matching vertex counts in a separate SVG under `rig/overrides/` so `normal <-> angry/curious/shy` morph instead of crossfading; extend `parts.map.json` with an `overrides` source.

## Maintenance

Deferred cleanups found in the foundation and agent-loop reviews. None blocks a phase; take them when touching the area.

- [x] **svgTarget renders the exported file.** `svgTarget.renderFrame` re-runs `exportSvg` in memory, so parity never tests the bytes in `dist/svg`. Load the written file instead.
- [x] **Move `Renderer` out of `packages/verify`.** `rig-extract` imports the test harness to render previews. Put `Renderer` in a shared package both can depend on.
- [x] **`readFill` accepts only 6-digit hex.** In `svg-source.ts` anything else (3-digit hex, `rgb()`, named colours) silently falls back to black; parse them or fail loudly.
- [x] **`svg-source` drops `Q` quadratics.** Lower them to cubics, or fail with a message naming the offending command and path id.
- [x] **`isPathData` is a whitelist, not a grammar.** It accepts malformed data such as a lone `M` with no coordinates. Parse with `parsePath` instead.
- [x] **`REST` vectors in `sample.ts` are shared instances.** `REST.position` and `REST.scale` are handed straight to callers; a mutating consumer would corrupt every future sample. Freeze them or return copies.
- [x] **Selector suffix errors conflate two failures.** `id#foo` reports "subpath index out of range"; distinguish "not a number" from "out of range".
- [x] **Typed loader for JSON rigs.** Replace the `as unknown as Rig` casts in `motion/index.ts` with a loader that validates and narrows.
- [x] **Repository LICENSE.** Decide the licence for the code and add the file; the artwork stays CC BY-SA 4.0 under its own attribution.
- [x] **`git_commit` re-runs the suite the reviewer just ran.** Every item pays for `npm run check` twice. Cache it by tree: `run_checks` writes the `git write-tree` hash it verified, and `git_commit` skips its own run while the tree still hashes the same.
- [x] **Exercise `.github/workflows/agent.yml`.** A remote now exists (`github.com/moreal/pubnyan-live`), but it's empty (no pushed refs) and has no `ANTHROPIC_API_KEY` secret, so an actual `workflow_dispatch` run isn't possible from this environment without inventing credentials or pushing someone else's `main` unasked. Decided the PR mechanism instead: **plain `git push` + `gh pr create`**, not `peter-evans/create-pull-request`. Reason: `git_commit` already commits each completed item directly onto `agent/backlog` inside the workflow's own checkout, so by the time the PR step runs the working tree is clean; `peter-evans/create-pull-request` only opens/updates a PR from a diff *it* commits, so it would silently no-op against an already-clean tree. The workflow now pushes `agent/backlog` and runs `gh pr create` (skipping if a PR is already open) using `GITHUB_TOKEN`. Remaining manual step for whoever has push access to the remote: add the `ANTHROPIC_API_KEY` secret, push `main`, then dispatch the workflow once with `max: 1` and read the PR it opens.
- [x] **backlog:add CLI.** Add `scripts/backlog-add.mjs` (`npm run backlog:add`) so a human or a Claude Code session can add items with the same checks the Planner gets. It reads one or more ops as JSON from a file or stdin (`{"summary": ..., "ops": [...]}`, the exact `write_backlog` input shape), runs them through `writeBacklog` from `src/tools/backlog-planner.ts` with a `PlannerFs` built on `node:fs` and `execSh`, and prints the resulting sha or the rejection errors. Refuses off `main` and on a dirty `docs/backlog.md` exactly like the tool. Document it in AGENTS.md next to `npm run plan` and in `src/skills/backlog-authoring/SKILL.md` as the non-Planner path. Done when: `echo '{"summary":"smoke add","ops":[{"op":"insert","section":"Maintenance","title":"Smoke: add cli.","text":"x. Done when: y."}]}' | npm run -s backlog:add` prints a sha and `git log -1 --format=%s` is `docs(backlog): smoke add`; a second run with the same title prints the duplicate-title rejection and exits non-zero; a unit test in `scripts/` or `src/tools/` covers both paths.
- [x] **storybook: scaffold.** Add devDeps `storybook`, `@storybook/html-vite`, `vite`; `.storybook/main.ts` (framework `@storybook/html-vite`, `stories: ['../stories/**/*.stories.ts']`, `staticDirs: ['../dist']`) and `.storybook/preview.ts` (white + dark backgrounds, `layout: 'centered'`); npm scripts `storybook:assets` (`npm run export:svg && npm run export:lottie && npm run export:rive && npm run export:video`, run as `prestorybook` and `prestorybook:build` so the gallery never shows stale artifacts), `storybook` (dev, port 6007), `storybook:build` (`storybook build -o dist/storybook`), and `storybook:smoke` (`node scripts/storybook-smoke.mjs`). Add `dist/storybook/` to `.gitignore`. `scripts/storybook-smoke.mjs` builds storybook, serves `dist/storybook` on a local port, and for every id in an exported `EXPECTED_STORY_IDS` list asserts the id exists in `dist/storybook/index.json`, opens `iframe.html?id=<id>&viewMode=story` in puppeteer, waits 1 s, screenshots and fails if the canvas is uniformly one colour; it prints one line per id and exits non-zero on the first failure. First story `stories/svg.stories.ts`, title `Targets/SVG`: `Player` (arg `clip`, a select over the clip names from `#motion/index.ts`, showing `/svg/<clip>.svg` in an `<img>`) and `Gallery` (a CSS grid of all clips). Done when: `npm run storybook:smoke` exits 0 and prints `targets-svg--player` and `targets-svg--gallery` as OK.
- [~] **storybook: video stories.** `stories/video.stories.ts`, title `Targets/Video`: `Player` (args `clip` select and `format` select over `mp4`/`webp`/`gif`; mp4 in `<video autoplay loop muted playsinline controls>`, webp/gif in `<img>`, all from `/video/`) and `Gallery` (grid of every clip's mp4). Note in the file that these files come from `npm run export:video` and need `ffmpeg` on PATH. Add `targets-video--player` and `targets-video--gallery` to `EXPECTED_STORY_IDS`. Done when: `npm run storybook:smoke` exits 0 and prints both new ids as OK.
- [ ] **storybook: lottie stories.** `stories/lottie.stories.ts`, title `Targets/Lottie`: `Player` (arg `clip` select; `lottie-web`'s `loadAnimation` with `path: '/lottie/<clip>.json'`, `renderer: 'svg'`, plus number arg `speed` and a `loop` bool, destroying the instance on story re-render) and `Bundle` (loads `/lottie/pubnyan.lottie` with `@lottiefiles/dotlottie-web` on a `<canvas>`, an `animation` select filled from the manifest at runtime, and a caption listing the bundle's animations). Add `targets-lottie--player` and `targets-lottie--bundle` to `EXPECTED_STORY_IDS`. Done when: `npm run storybook:smoke` exits 0 and prints both new ids as OK.
- [ ] **storybook: rive stories and state machine.** Extend `packages/export-rive/src/cli.ts` to also write `dist/rive/pubnyan.riv` via `exportRiveMachine(getRig(machine.rig), clips.filter(c => c.rig === machine.rig), machine)`. `stories/rive.stories.ts`, title `Targets/Rive`: `Player` (arg `clip` select, `@rive-app/canvas` on a `<canvas>` at artboard size playing `/rive/<clip>.riv`) and `Machine` (loads `/rive/pubnyan.riv` with `stateMachines: 'main'` and derives its controls from `#motion/machine.ts`: the `expression` enum as a select writing the value's index into the `StateMachineNumber`, `loading` as a bool, and each trigger input as a button in the toolbar/args that calls `.fire()`; it also renders the live `stateMachineInputs('main')` names under the canvas). Add `targets-rive--player` and `targets-rive--machine` to `EXPECTED_STORY_IDS`. Done when: `npm run storybook:smoke` exits 0 and prints both new ids as OK, and `npm run export:rive` writes `dist/rive/pubnyan.riv`.
- [ ] **storybook: gh-pages deploy.** `.github/workflows/storybook.yml`: on push to `main` and on `workflow_dispatch`, checkout with submodules, node 26, `npm ci`, install `ffmpeg` (`sudo apt-get install -y ffmpeg`), `npm run rig:extract`, `npm run storybook:build`, then `actions/upload-pages-artifact` on `dist/storybook` and `actions/deploy-pages` with `permissions: { pages: write, id-token: write }` and a `github-pages` environment. Document the published URL and the one-time "enable Pages: GitHub Actions" step in `README.md`. Done when: `npx --yes @action-validator/cli .github/workflows/storybook.yml` (or `node -e` parsing it with a YAML parser) reports no error and the file's build step is exactly `npm run storybook:build` with the artifact path `dist/storybook`.
